import Navbar from '../Navbar/Navbar';
import Stays from '../Stays/Stays';
import './Main.css'
const Main = () => {
    return ( 
        <main>
            <Navbar/>
            <Stays/>
        </main>
    );
}
export default Main;